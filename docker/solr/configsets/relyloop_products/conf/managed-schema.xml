<?xml version="1.0" encoding="UTF-8"?>
<!--
SPDX-FileCopyrightText: 2026 soundminds.ai

SPDX-License-Identifier: Apache-2.0

infra_adapter_solr Story A10: managed-schema for the demo `products` collection.
uniqueKey=id; mirrors the field shape of samples/products.json so the seed
script can index directly.
-->
<schema name="relyloop_products" version="1.7">
  <uniqueKey>id</uniqueKey>

  <fieldType name="string" class="solr.StrField" sortMissingLast="true" docValues="true" />
  <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100">
    <analyzer type="index">
      <tokenizer class="solr.StandardTokenizerFactory" />
      <filter class="solr.LowerCaseFilterFactory" />
      <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt" />
    </analyzer>
    <analyzer type="query">
      <tokenizer class="solr.StandardTokenizerFactory" />
      <filter class="solr.LowerCaseFilterFactory" />
      <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt" />
      <filter class="solr.SynonymGraphFilterFactory" synonyms="synonyms.txt" ignoreCase="true" expand="true" />
    </analyzer>
  </fieldType>
  <fieldType name="pfloat" class="solr.FloatPointField" docValues="true" />
  <fieldType name="pint" class="solr.IntPointField" docValues="true" />
  <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true" />

  <!-- Fields below mirror the exact shape of samples/products.json
       (id, title, description, bullet_points[list], brand, color). price /
       category / in_stock are kept as optional declared fields for operator
       data that includes them, but are absent from the demo sample. -->
  <field name="id" type="string" indexed="true" stored="true" required="true" />
  <field name="title" type="text_general" indexed="true" stored="true" />
  <field name="description" type="text_general" indexed="true" stored="true" />
  <field name="bullet_points" type="text_general" indexed="true" stored="true" multiValued="true" />
  <field name="brand" type="string" indexed="true" stored="true" />
  <field name="color" type="string" indexed="true" stored="true" />
  <field name="price" type="pfloat" indexed="true" stored="true" />
  <field name="category" type="string" indexed="true" stored="true" />
  <field name="in_stock" type="boolean" indexed="true" stored="true" />
  <field name="_version_" type="pint" indexed="true" stored="true" />

  <!-- Catch-all: any field not declared above is accepted as a stored,
       searchable text field. Mirrors the ES/OpenSearch seed's dynamic
       mapping (seed_es.py) so a new column in products.json never 400s the
       Solr seed with "unknown field". -->
  <dynamicField name="*" type="text_general" indexed="true" stored="true" multiValued="true" />
</schema>
