# RelyLoop — Active State

> Read this first. A one-page snapshot: current focus, the last few merges, what's in flight, what's queued, and where the project sits in the MVP1 → MVP2 → MVP3 → GA roadmap. **Historical feature-merge narrative + chained execution context lives in [`state_history.md`](state_history.md)** — new merge entries land there, not here (per `chore_state_md_size_compression`, 2026-05-29). Keep this file loadable in a single `Read` call.

**Last updated:** 2026-06-09 (**`feat_query_normalizer_typed_pipeline` merged** — PR #509, squash-merged `7a24849`. Extends Phase 1's fixed-bundle `query_normalizer` into a **typed pipeline**: declare `{type:"normalizer_pipeline", steps:[…]}` over 6 atomic `NormalizerStep`s; the Optuna loop searches the **powerset** (`2^N` labels). Bundles stay backward-compatible (one engine — `normalize_pipeline`; `normalize(bundle)` is now a wrapper). FR-3 smart-quote (U+2019) expansion, reserved-key-only validation (`NormalizerPipelineMisplacedError` → `INVALID_SEARCH_SPACE`), adapter pre-render hook resolves bundle OR pipeline label via `steps_for_label`, **bilingual** (Python+JS) PR-body snippets with three-way output parity (committed corpus fixture), `<RowNormalizerPipeline>` builder row + 7 glossary keys + digest-advisory broadening, docs. **No migration** (head stays `0023`). Q-1 locked (include `expand_contractions_custom` inert — 6 steps); Q-2 locked (JS parity via frontend vitest fixture). Opus self-review (GPT-5.5 unreachable); Gemini 2 Med accepted (`7047190`: strip_punctuation snippets use the runtime regex). Extended `verify_enum_source_of_truth` to resolve StrEnum classes. All 19 `pr.yml` checks green (smoke skipped). Full narrative in [`state_history.md`](state_history.md). Previously: **`bug_cluster_url_ssrf_hostname_bypass` Phase 1 merged** — PR #510, squash-merged `3cb28c7`; closes the cluster `base_url` SSRF hostname-bypass via a flag-gated async resolve-and-classify guard before any probe. New `domain/cluster/url_policy.py` (pure classifier) + `services/cluster_url_policy.py` (async orchestrator) + `400 CLUSTER_URL_BLOCKED`; the two `base_url` validators de-duped to one structural helper. No migration/UI (Alembic head stays `0023`). Surfaced by security review #504 (auto-closed); driven through `/pipeline --auto` (spec→plan→3 stories). 52 new tests; Gemini 2 Med accepted (bounded DNS, malformed-port 422). **Phase 2 (connect-time IP pinning) deferred — folder stays in `planned_features/02_mvp2/` with `phase2_idea.md`, NOT archived.** Previously: **`chore_overnight_result_card_screenshot` finalized** — PR #492 reviewed via `/pr-review` and squash-merged (`4128572`); this entry is its `state.md`/`state_history.md` finalization. Ships the tutorial Step 12 morning-result-card PNG + the first-ever tutorial-image **ferry plumbing** across both doc-copy pipelines (`ui/scripts/copy-docs.mjs` `copyImageAssets`/`pruneStaleImages`; `website/scripts/build_guides.py` `copy_long_form_images()` + a back-compat-defaulted prune kwarg), both guarding the `images/` subdir from the flat `.md`/`rmtree` prune and no-op'ing when the source dir is absent. **Docs/test/script only — no code-path/API/migration** (Alembic head stays `0023`). 17 new tests (10 vitest + 7 pytest). GPT-5.5 unreachable → Opus self-review; Gemini 1 Med accepted (`02450dd`: hoist `readFileSync` to a top-level import vs inline `require`). All three freshness gates (`generated-artifacts-fresh`/`copy-docs`/`build-guides`) + 18/19 checks green (smoke skipped — opt-in/off). Full narrative in [`state_history.md`](state_history.md).)

## Where the roadmap sits

MVP1 (v0.1) **shipped** — all six differentiators live (Bayesian/TPE optimizer, Git-PR apply path, conversational agent, ES + OpenSearch adapters, LLM judgments, local-first stack). The release matrix was compressed to four stops on 2026-05-27 — MVP1 → MVP2 (Three-Engine + Real Signals: Solr adapter + UBI judgments) → MVP3 (Observable) → GA v1 (hardening). Multi-tenant + multi-LLM + multi-Git + LTR + Path B are backlog. Canonical matrix: [`docs/01_architecture/tech-stack.md`](docs/01_architecture/tech-stack.md); full reshuffle rationale archived in [`state_history.md`](state_history.md).

## CI note — full suite active (smoke is opt-in)

**Heavy CI is ON.** `SKIP_HEAVY_CI` was deleted 2026-05-31 — the full `pr.yml` suite (backend lint/typecheck/tests/coverage, frontend, both `docker buildx`) runs on every PR. The earlier ~3-day skip (2026-05-29, PR #307) was a private-repo GHA-budget measure; it no longer applies now that the repo is **public** — standard GitHub-hosted runners (`ubuntu-latest`/`ubuntu-24.04`, all jobs) are unlimited-free on public repos. The `if:` kill-switch stays in `pr.yml` as documented infra; re-enable only with a fresh budget reason by setting `SKIP_HEAVY_CI=true`.

**The `smoke` job is OFF by default (2026-06-02).** Gated behind a `SMOKE_TEST` repo variable: `if: ${{ vars.SMOKE_TEST == 'true' && vars.SKIP_HEAVY_CI != 'true' }}`. Unset → false → smoke skipped. The reseed-runtime block that originally triggered the OFF default was cleared by [`infra_smoke_reseed_runtime_budget`](docs/00_overview/planned_features/02_mvp2/infra_smoke_reseed_runtime_budget/feature_spec.md) — `demo-ubi.spec.ts` is now CI-excluded via `ui/playwright.config.ts`'s `testIgnore` CI branch, so the smoke job's Playwright runtime is expected to fit within the 25-min cap. Operator may re-enable per-PR smoke signal after verifying per the spec's §16 manual `playwright test --list` check; SMOKE_TEST stays operator-controlled rather than auto-on because the smoke job is the only check that boots the full Compose stack. Opt in with `gh variable set SMOKE_TEST --body true`; disable (default) with `gh variable delete SMOKE_TEST`. This retires the "smoke red/cancelled on every PR → merge on the D-6 fast lane" friction — there's no longer a smoke check to fast-lane past.

## Current branch / execution context

- **Branch:** `main` (`chore_install_tls_hint_recommend_extract` just merged — PR #527, 2026-06-16; before it `chore_corp_ca_extract_make_target` PR #525, `chore_corp_install_dx_improvements` PR #523, `chore_dockerfile_remove_syntax_directive` PR #521, `chore_dockerfile_http_proxy_args` PR #519 — finishes the corp-network install story by syncing the two stale TLS-hint surfaces with the auto-extract recommendation). All `pr.yml` checks green (smoke skipped — opt-in/off). Heavy backend CI check is named `backend (tests + coverage)`.
- **Active feature:** None in flight. **Deferred phase tracked:** `bug_cluster_url_ssrf_hostname_bypass` **Phase 2** (connect-time IP pinning for DNS rebinding) remains in `planned_features/02_mvp2/bug_cluster_url_ssrf_hostname_bypass/phase2_idea.md` (folder intentionally NOT archived since a phase is pending). **Still deferred:** `chore_demo_seeding_integration_tests_rewrite` (14-story DB-only integration choreography; blocked on a local stack for safe CI-blind validation) and `infra_pr_yml_split_backend_test_lanes` (defer-until-integration-is-the-binding-CI-constraint). GPT-5.5 unreachable in this env → Opus self-review substitution.
- **Alembic head:** `0023_proposals_superseded_status` (unchanged — `feat_fts_rank_ordering` is no-migration; head last moved by `feat_overnight_final_solution_phase3` PR #457).
- **Python:** 3.13. **Frontend stack:** Next 16 (App Router + Turbopack), React 19, Tailwind 4 (CSS-first), Vitest 4, ESLint 9 (flat), TypeScript 6, Playwright (chromium, single worker) for E2E.
- **Coverage gates:** backend 80% (`fail_under` in pyproject), UI vitest + tsc + ESLint + Next build, plus a full-stack smoke E2E job. Live pass counts: see the latest `pr.yml` run (the historical per-feature counts moved to `state_history.md`).

## Last 5 merges (newest first)

Detail + reasoning for each is in [`state_history.md`](state_history.md).

- **2026-06-16** — `chore_install_tls_hint_recommend_extract` (PR #527, squash-merged `3e85d2a`). **Sync the two stale TLS-hint surfaces with `make corp-ca-extract`.** Tiny follow-up to PR #525: after the auto-extract make target landed, two surfaces still recommended the **manual** `cp /path/to/corp-ca.crt ./secrets/corp_ca.crt` workflow as the primary fix — `scripts/install.sh`'s `diagnose_build_failure` TLS pattern and `docs/01_architecture/deployment.md` "Corporate TLS interception" §"One-time setup" example. Both reworded to lead with `make corp-ca-extract` and demote manual `cp` to a clearly-labeled "Fallback (rare)" / "manual fallback" block. The runbook + `.env.example` were already updated in PR #525. **Docs/infra only — no code-path/API/migration** (Alembic head stays `0023`). GPT-5.5 unreachable → Opus self-review. **Gemini caught 2 real MED issues + 0 stale on the fix commit.** (1) `scripts/install.sh:147` em-dash in CLI output — real portability concern under `LANG=C` or older terminals (both common in strict corp environments where the TLS hint is most likely to fire); switched the em-dash I introduced to ASCII hyphen. Scope-noted that other em-dashes in install.sh source comments aren't user-facing + two pre-existing echos for `RELYLOOP_SKIP_BUILD` / `RELYLOOP_SKIP_AUTO_SEED` weren't in this PR's scope. (2) `docs/01_architecture/deployment.md:273` narrow `/Library/Keychains/System.keychain` path on the macOS `security find-certificate` command — corp IT often installs CAs into the user's login keychain, not System; dropping the path lets `security` use the default keychain list (login + system). Same fix applied to the analogous example in `docs/03_runbooks/corporate-network-install.md:168` for consistency. Both findings accepted in `b1e6b231`; CI re-ran clean (10/10 + smoke skipped). 0 stale Gemini re-flags on the fix commit (clean pattern this round). Merge-skew clean. PR #527 also illustrates the compounding-quality property of the doc-as-code stack: PR #525 shipped the new capability + flipped some surfaces; PR #527 closed the last two stale surfaces caught by reading the live diagnostic, and `/code-review` improved the fix itself.
- **2026-06-16** — `chore_corp_ca_extract_make_target` (PR #525, squash-merged `55f2bf0`). **Auto-discover corp CA via TLS probe — `make corp-ca-extract`.** Closes the operator-friction gap left by PR #523: instead of asking operators to `cp /path/to/corp-ca.crt ./secrets/corp_ca.crt` (still requires finding the cert on Linux trust paths / extracting from macOS keychain / exporting from Chrome), this PR's new Makefile target probes the live TLS chain on the operator's network and saves the corp root CA automatically. **Docs/infra only — no code-path/API/migration** (Alembic head stays `0023`). Single-script PR, ~242 lines in `scripts/corp-ca-extract.sh`. Mechanism: `openssl s_client -showcerts -connect <probe>:443` returns the cert chain the proxy presents — the LAST cert in the chain is typically the root used to sign everything else. The script walks the chain, checks the last cert's Subject against ~27 well-known public CA issuer fragments (DigiCert, ISRG/Let's Encrypt, Google Trust Services, GlobalSign, Sectigo, Amazon Root CA, …); if it matches a public root → "No corporate TLS interception detected" + exit cleanly; if it doesn't match → saves to `./secrets/corp_ca.crt` with the Subject printed for verification. Idempotent (skips file write if existing content matches). `PROBE_HOST` / `PROBE_PORT` / `TARGET` env vars override defaults. New `make corp-ca-extract` target with `## …` doc-comment auto-discovered by `make help`. Updated `docs/03_runbooks/corporate-network-install.md` §2 to lead with the auto-extract flow; manual `cp` demoted to "Fix — manual fallback" with an expanded source list (Linux trust paths, macOS `security find-certificate -p -c "name" /Library/Keychains/System.keychain` one-liner, Chrome/Firefox export). `.env.example` corp CA block now recommends `make corp-ca-extract` first. **Gemini ran twice, all 5 findings adjudicated.** First commit: 1 HIGH (proxy routing + CR stripping bundled — script's whole audience is corp users behind explicit `HTTPS_PROXY` proxies; without `-proxy host:port` the probe never leaves a strict corp firewall; also Windows/WSL CRLF streams broke the awk anchor) + 1 MED (`PROBE_HOST` URL/port defensive cleanup for `https://corp/`-style pastes) — all 3 accepted in `8467fe09`. Bash 3.2 footnote: the naive `"${proxy_args[@]}"` empty-array expansion fails on macOS default bash 3.2 under `set -u` ("arr[@]: unbound variable") — used bash-3.2-safe form `${arr[@]+"${arr[@]}"}` (already used elsewhere in the repo per `parallel-worktrees.md`); verified by direct bash 3.2 test. Follow-up Gemini on `8467fe09`: 2 stale re-flags of the exact-same prior suggestions — both rejected post-merge with cited counter-evidence (`proxy_args` at line 69, `tr -d '\r'` at line 89, URL cleanup at lines 44-50 all demonstrably in main). All `pr.yml` checks green (10/10 passed + smoke skipped — opt-in/off); merge-skew clean. **Closes the five-PR corp-network install story** (#517 registry ARGs, #519 HTTP proxy + UV→GHCR rename, #521 syntax-directive removal, #523 CA cert build secret + diagnostics + runbook, #525 auto-extract make target). Operators behind corp firewalls now have the full path: edit 5 lines in `.env`, run `make corp-ca-extract && make up`. No file hunting, no keychain spelunking, no manual `cp`.
- **2026-06-16** — `chore_corp_install_dx_improvements` (PR #523, squash-merged `6c5fac5`). **Corp-network DX — optional CA cert build-secret + actionable build-failure diagnostics + symptom-first runbook.** Three bundled improvements that turn the corp-firewall install experience from "paste error into Google" into "actionable hint with doc pointer": (1) **Optional `./secrets/corp_ca.crt`** mounted as a BuildKit secret (`--mount=type=secret,id=corp_ca,required=false`) in both Dockerfiles; when non-empty, copied to `/usr/local/share/ca-certificates/` and `update-ca-certificates` rebuilds the trust bundle so every HTTPS tool (npm, pnpm, uv, pip, curl, runtime OpenAI/GitHub/cluster clients) trusts the corp CA at both build time AND runtime. Compose wires `build.secrets: - corp_ca` into all 4 services (migrate/api/worker/ui) + new top-level `secrets.corp_ca: {file: ./secrets/corp_ca.crt}` entry; `scripts/install.sh` auto-creates an empty placeholder so Compose's secrets validation doesn't fail at startup. Fixes the `SELF_SIGNED_CERT_IN_CHAIN`/`unable to get local issuer certificate`/`CERTIFICATE_VERIFY_FAILED`/`x509: certificate signed by unknown authority` family of TLS-interception errors. Empty placeholder = no-op for OSS users. (2) **`diagnose_build_failure` wrapper in `scripts/install.sh`** wraps `docker compose build` (via a `do_compose_build()` function so the bare `docker compose build` line satisfies the existing `verify_install_builds_all_services` CI guard's regex anchor), captures output to a `mktemp` file (cleanup via `trap EXIT`), and on failure scans for 3 corp-network failure-signature patterns (TLS interception, registry blocked, egress/DNS) printing a tailored hint + runbook-section pointer. Fail-open fallback prints a generic pointer when no pattern matches. (3) **New `docs/03_runbooks/corporate-network-install.md` runbook** (~328 lines, symptom-first FAQ) — 5 §sections covering registry pull failures, TLS verification errors, egress/DNS failures, worker-stays-unhealthy (no_proxy missing Compose service names), runtime egress fails — each with error block, cause, fix, verification, link to architecture doc. Plus quick decision tree at the top, a verifying-your-full-config one-shot, Artifactory namespace-layout disambiguation (unified vs split per upstream), and cross-refs to deployment.md (architecture). Cross-references added to `CLAUDE.md` "Key Runbooks" table, `local-dev.md` "Stack will not start" troubleshooting, and `docs/01_architecture/deployment.md` new "Corporate TLS interception" §. **Docs/infra only — no code-path/API/migration** (Alembic head stays `0023`). GPT-5.5 unreachable → Opus self-review. **Gemini findings + adjudication.** Initial review: 7 findings (HIGH ENV-anti-pattern, MED host.docker.internal) — see PR #519 context (carried over via memory). On this PR: 4 MEDIUM findings on the first commit set, all accepted (3 `update-ca-certificates | tail -1` pipes silently masking exit codes in /bin/sh-without-pipefail; 1 `trap EXIT` cleanup mechanism). Follow-up: 4 stale re-flags on the fix commit — all rejected with cited counter-evidence (`grep` confirmed each fix was already present). CI green chronology: 1st run failed (CI guard `verify_install_builds_all_services.sh` rejected the `if ! docker compose build | tee ...` form because the regex requires the bare command line); 2nd run passed after refactoring to `do_compose_build()` function wrapper; 3rd run passed after Gemini-driven `update-ca-certificates` pipe removal + `trap EXIT`. Three adjudication summary tables posted on the PR per CLAUDE.md. Six commits in the branch squash-merged: A (CA cert support), B (diagnostic wrapper), C (runbook), D (CLAUDE.md index), guard-regex fix, Gemini adjudication. **Closes the four-PR corp-network install story** (#517 registry ARGs, #519 HTTP proxy + UV→GHCR rename, #521 syntax-directive removal, #523 CA cert + DX) — operators behind a corp firewall now have a fully-supported `make up` path with 5 env vars + 1 cert file + a runbook to diagnose anything that goes wrong.
- **2026-06-16** — `chore_dockerfile_remove_syntax_directive` (PR #521, squash-merged `615b7cf`). **Unblock corp-firewall `make up` by removing the `# syntax=docker/dockerfile:1.7` directive from both Dockerfiles.** Surfaced by a live reproducer: inside a corporate firewall configured to route all Docker traffic through an Artifactory proxy at `fcr.fmr.com`, `make up` failed at line 1 of the Dockerfile parse with `failed to resolve source metadata for docker.io/docker/dockerfile:1.7: 403 Forbidden` — **before** any `ARG` substitution was in scope, so `BASE_REGISTRY=fcr.fmr.com/` couldn't redirect it. The `# syntax=…` line is parsed by BuildKit first (it tells BuildKit which Dockerfile frontend image to fetch to parse the file), so there's no ARG-substitution-based fix possible at that layer. Without the directive, BuildKit uses the frontend embedded in the BuildKit daemon — no image fetch at all, no docker.io request. Safe because we don't use any BuildKit-1.7+-specific features: only stable directives (multi-stage, ARG, COPY --from=<stage>, LABEL, HEALTHCHECK, OCI labels, ${ARG} in FROM) are used; no RUN --mount=type=cache/secret/ssh. OSSF Scorecard's PinnedDependencies posture is unchanged (it looks at FROM @sha256: digests, not the syntax line). Impact: zero for OSS users (embedded frontend handles all our directives identically); unblocks every corp-firewall user behind an Artifactory-style proxy. **Docs/infra only — no code-path/API/migration** (Alembic head stays `0023`). 4-line deletion total across the two Dockerfiles. GPT-5.5 unreachable → Opus self-review; zero Gemini findings (clean small PR). All `pr.yml` checks green (10 jobs success + smoke skipped — opt-in/off). Validation: buildx `--check` output is SHORTER post-fix because the `[internal] load metadata for docker.io/docker/dockerfile:1.7` step (the exact step that was 403'ing) is gone. This is the **third PR in the corp-proxy install story** (#517 added registry-prefix ARGs; #519 added HTTP proxy + UV→GHCR rename; #521 removed the last docker.io reference that ARGs couldn't reach). PR opened mid-conversation as an ad-hoc fix after the user reported the failure from inside their corp firewall.
- **2026-06-16** — `chore_dockerfile_http_proxy_args` (PR #519, squash-merged `4ff6e33`). **Corporate HTTP-proxy support (build + runtime egress) + `UV_REGISTRY` → `GHCR_REGISTRY` rename.** Two related Dockerfile-flexibility changes bundled. (1) Adds `http_proxy` / `https_proxy` / `no_proxy` wiring so corp installs behind an HTTP proxy route apt / PyPI / npm at build time AND outbound HTTP from the runtime container (OpenAI, GitHub, registered cluster HTTP) through that proxy — single set of vars in `.env` covers both paths. Architecture: build-time uses Docker's [predefined proxy ARGs](https://docs.docker.com/build/building/variables/#predefined-args) (BuildKit auto-forwards from `--build-arg` to RUN env, no Dockerfile `ARG`/`ENV` needed, excluded from `docker history` so URL never bakes into image); runtime uses each Compose service's `environment:` block. (2) Renames `UV_REGISTRY` → `GHCR_REGISTRY` — describes *what upstream it overrides* (the GHCR namespace) rather than coupling to a specific consumer (uv); matches `BASE_REGISTRY` naming; forward-compatible for any future GHCR image. `.env.example` recommends `no_proxy` defaults that bake in Compose service names (worker→ES routing gotcha) + `host.docker.internal` (local-LLM-via-Ollama gotcha) + `10.0.0.0/8` (VPC) + `169.254.169.254` (cloud metadata). **Docs/infra only — no code-path/API/migration** (Alembic head stays `0023`). GPT-5.5 unreachable → Opus self-review. **Initial design had two layers of issue Gemini caught accurately:** (a) the first commit (`03cb1e3c`) added explicit `ARG http_proxy=` + `ENV http_proxy=…` blocks to both Dockerfiles — both redundant (BuildKit predefined-ARGs handle build-time) AND counterproductive (`ENV` bakes the proxy URL into the runtime image, visible in `docker inspect`, environment-coupling the image). Accepted (`77f81542`): removed all Dockerfile `ARG`/`ENV` proxy blocks; added `environment:` proxy entries to all 4 Compose services (migrate / api / worker / ui); image stays portable. (b) `host.docker.internal` was missing from the default `no_proxy` — a sharp catch for local-LLM dev (Ollama / LM Studio / vLLM via `OPENAI_BASE_URL=http://host.docker.internal:…`) behind a proxy. Added across all three doc surfaces (Dockerfile comment, .env.example, deployment.md). Two follow-up Gemini findings on `77f81542` (re-flagging the fixed compose `environment:` shape and the already-present `host.docker.internal`) **rejected as stale** with cited counter-evidence (8 occurrences of `http_proxy: ${http_proxy:-}` for the build.args + environment dual-wiring; 4 grep matches for `host.docker.internal` in Dockerfile). Both adjudication summary tables posted on the PR per CLAUDE.md. UV→GHCR rename is intentionally a hard rename (not a deprecation shim): PR #517 had merged hours earlier, so the breaking-change surface was effectively zero. `state.md`'s PR #517 entry intentionally retains the original `UV_REGISTRY` wording (accurate for what shipped at that merge); this entry explains the rename. All `pr.yml` checks green (18 passed, smoke skipped — opt-in/off); merge-skew check clean. Three commits squash-merged: `03cb1e3c` (initial HTTP_PROXY + ARG/ENV), `9a9f1a0c` (UV→GHCR rename), `77f81542` (Gemini adjudication: removed ARG/ENV, added compose environment:, added host.docker.internal).
_(older entries — full narrative in [`state_history.md`](state_history.md): `chore_dockerfile_corp_proxy_args` PR #517, `feat_query_normalizer_typed_pipeline` PR #509, `bug_cluster_url_ssrf_hostname_bypass` Phase 1 PR #510, `chore_overnight_result_card_screenshot` PR #492, `bug_seed_meaningful_demos_silent_bulk_errors` PR #482, `bug_relyloop_spec_ubi_section_drift` PR #481, `chore_demo_reseed_partial_completion_fast_test` PR #480, `chore_pr_yml_parallelize_backend_job` PR #478, `chore_studies_post_arq_spy_fixture` PR #476, `chore_ubi_reader_search_after_pagination` PR #474, `feat_fts_rank_ordering` PR #472, `bug_judgment_header_omits_click_bucket` PR #470, `bug_baseline_phase_test_isolation` PR #466, `chore_cluster_detail_rung_badge` PR #464, `feat_ubi_llm_study_comparison` PR #461, `feat_query_normalization_tuning` PR #459, `feat_overnight_final_solution_phase3` PR #457, `feat_study_wizard_inline_judgment_generation` PR #453, `feat_walkthrough_video_cursor_captions` PR #451, `feat_website_walkthrough_guides` PR #448, `feat_proposal_full_param_space_view` PR #446, `feat_overnight_studies_summary_card` PR #444, `feat_overnight_final_solution_phase2` PR #442, `feat_overnight_final_solution` PR #440, `feat_studies_list_trial_convergence_columns` PR #438, `feat_list_count_columns` PR #436, `infra_generated_artifact_freshness_gate` PR #433, `chore_scorecard_pin_deps_postcss` PR #430, `bug_llm_capability_cache_no_refresh` PR #426, `infra_smoke_reseed_runtime_budget` PR #424, `feat_studies_convergence_visibility` PR #421/#422, `bug/cli-seed-ubi-missing-engine-type` PR #419, `chore_template_library_expansion` PR #416, `infra_solr_smoke_stability` PR #383, `infra_solr_ci_readiness` Phase 1 PR #367, MVP2 backlog batch PR #364, `feat_study_convergence_indicator` PR #352, `feat_overnight_autopilot` PR #343, `infra_adapter_solr` PR #336, …)_

## In flight

- **3-chore queue (2026-06-05 afternoon):** #1 `chore_studies_post_arq_spy_fixture` **shipped** (PR #476); #3 `chore_pr_yml_parallelize_backend_job` **shipped** (PR #478, descoped to lint-dedup; lane-split deferred to `infra_pr_yml_split_backend_test_lanes`). **#2 `chore_demo_seeding_integration_tests_rewrite` deferred** (14-story DB-only integration choreography; blocked on a local stack for CI-blind-safe validation). (Earlier 3-feature queue fully shipped: PR #470/#472/#474.)
- **Plan-stage, `/impl-execute`-ready (no gates):** `chore_demo_seeding_integration_tests_rewrite` (PR #286, DB-only integration — CI-only verification). **Note:** `feat_query_normalizer_typed_pipeline` **shipped** (PR #509, 2026-06-09 — Q-1 + Q-2 locked, design-ahead gate cleared). Its sibling `feat_apply_path_normalizer_declaration` remains **product-gated** (G-2 operator-friction evidence) + design-ahead. (`chore_arq_pool_aclose_deprecation`, `chore_cluster_detail_rung_badge`, `bug_baseline_phase_test_isolation`, `bug_judgment_header_omits_click_bucket`, `chore_ubi_reader_search_after_pagination`, `chore_studies_post_arq_spy_fixture`, `chore_pr_yml_parallelize_backend_job` shipped — PRs #463/#464/#466/#470/#474/#476/#478.)

## Queued (priority-ordered by dashboard / dep graph)

**Source of truth:** [`docs/00_overview/DASHBOARD.md`](docs/00_overview/DASHBOARD.md) + [`docs/00_overview/MVP1_DASHBOARD.md`](docs/00_overview/MVP1_DASHBOARD.md) (regenerated by the `mvp1-dashboard-regen` pre-commit hook). Run `/pipeline status` for the live view.

**MVP1 backlog is fully drained** (`01_mvp1/` empty as of PR #310). We're inside **MVP2 / v0.2 — "Three-Engine + Real Signals"**, now **3/3 scoped items done** (`feat_contextual_help_mvp2`, `feat_demo_ubi_study_comparison`, `infra_adapter_solr` — the Solr headliner shipped via PR #336 on 2026-05-31, completing the three-engine reach). The remaining MVP2 work is all Idea/Plan-stage (run `ls docs/00_overview/planned_features/02_mvp2/` for the live list + count):

- **Headliners (idea-stage):** `feat_fts_rank_ordering`, `feat_query_normalization_tuning`, `feat_study_convergence_indicator`, `feat_ubi_llm_study_comparison` (side-by-side UBI-vs-LLM study comparison view + deferred cluster-detail rung badge, P2 — Phase 2 split out of `feat_demo_ubi_study_comparison`), plus the Phase-2/3 split-outs from the 2026-05-31 planning batch still pending (`feat_query_normalizer_typed_pipeline`, `feat_apply_path_normalizer_declaration`). `feat_overnight_autopilot` shipped (PR #343, 2026-05-31); `feat_overnight_studies_summary_card` shipped (PR #444, 2026-06-04).
- **Bugs held for MVP2:** `bug_chat_long_conversation_truncation` (investigation `bug_fix.md` exists; pullable forward but deferred for scope discipline — latency-of-impact is zero today), `bug_webhook_concurrent_merge_race_timing_sensitive`. (`bug_seed_meaningful_demos_silent_bulk_errors` shipped — PR #482.)
- **Chores/infra:** `chore_auto_followup_parent_advisory_lock`, `chore_demo_seeding_integration_tests_rewrite`, `chore_template_library_expansion`, `chore_ubi_hybrid_template_render` (P3, vestigial-template contract cleanup — spun out of `feat_ubi_judgments`), `infra_arq_subprocess_test`, `infra_pr_yml_split_backend_test_lanes` (P2, deferred — lane-split + `coverage combine` + split-integration-by-service; defer-until-integration-is-the-binding-CI-constraint; spun out of `chore_pr_yml_parallelize_backend_job`). (`chore_ubi_reader_search_after_pagination` PR #474, `chore_studies_post_arq_spy_fixture` PR #476, `chore_pr_yml_parallelize_backend_job` PR #478 shipped.)

**Other buckets:** `03_mvp3/` (Observable — includes `infra_optuna_orphan_reaper`, deferred from MVP1 per spec §11 operational tolerance), `04_ga/`, `99_backlog/` (defer-until-incident items), `00_unsure/` (empty).

## Known debt / fragility

- ~~**LLM capability cache never refreshes after its 24h TTL (P1).**~~ — **RESOLVED** (`bug_llm_capability_cache_no_refresh`, PR #426, 2026-06-02). `read_or_recompute_capability_result()` recomputes on cache miss inline (Option A) with a per-worker `asyncio.Lock` single-flight; `_check_llm_preflight` opts in. Stacks up >24h now self-heal on the next judgment-generate POST instead of 503'ing until an api restart. `/healthz` + chat orchestrator stay read-only by design (D-5). Full narrative in [`state_history.md`](state_history.md).

- ~~**Solr is not CI-ready (P1) — `pr.yml` backend + smoke are red on every branch.**~~ — **RESOLVED across three PRs.** Backend half: `infra_solr_ci_readiness` Phase 1 (PR #367, 2026-06-01) made the demo reseed engine-tolerant. Solr stability: `infra_solr_smoke_stability` (PR #383, 2026-06-02) — Solr crashed on filesystem permissions (container UID 8983 vs root-owned `./data/solr/`); fixed inline as Lever 0 (`mkdir + chown` before `make up`) plus Lever 1 heap-cap, COMPOSE_PROJECT_NAME pin, and diagnostics fold-in. Reseed-runtime: `infra_smoke_reseed_runtime_budget` (this work, 2026-06-02) — `demo-ubi.spec.ts` CI-excluded via `ui/playwright.config.ts` `testIgnore` (Option A locked at preflight); local `make up` smoke retains coverage; nightly-CI caveat captured in `docs/03_runbooks/smoke-solr-stability.md` §5. Smoke job stays OFF by default per operator preference; flip `SMOKE_TEST=true` after the spec's §16 verification to restore per-PR signal.

- ~~**`backend/app/eval/qrels_loader.py` is an MVP1 stub.**~~ — **Resolved.** PR #35 replaced the stub with a real `SELECT query_id, doc_id, rating FROM judgments WHERE judgment_list_id = :id`. The legacy `JudgmentsTableMissing` symbol is retained as a no-op compat shim for any imported reference in older tests. Integration tests now seed real `judgments` rows; `run_trial` consumes the loader directly.
- **`infra_optuna_orphan_reaper`** — Phase 2 orchestrator can die between `study.ask()` and the enqueue commit, leaving orphan Optuna RUNNING trials. Operationally tolerated for MVP1 per spec §11 "Operational tolerance"; periodic reaper deferred to MVP3 ([`03_mvp3/infra_optuna_orphan_reaper`](docs/00_overview/planned_features/03_mvp3/infra_optuna_orphan_reaper/idea.md)).
- ~~**CI lacks a `make up` smoke job.**~~ — **Resolved.** `infra_ci_smoke_makeup` shipped 2026-05-13; `pr.yml` now has a full-stack `smoke:` E2E job (see the coverage-gates line above).
- **Tangential bugs captured during the bootstrap:**
  - ~~`bug_env_file_corrupted_during_session`~~ — **Resolved.** Defense-in-depth `.env*` filename CI guard shipped in PR #94 + folder finalized to [`implemented_features/2026_05_13_bug_env_file_corrupted_during_session/`](docs/00_overview/implemented_features/2026_05_13_bug_env_file_corrupted_during_session/). Original local-tooling rename event remains undetermined (user-side investigation open).
  - ~~[`chore_starlette_422_deprecation`]~~ — **Resolved.** Shipped 2026-05-13 ([`implemented_features/2026_05_13_chore_starlette_422_deprecation`](docs/00_overview/implemented_features/2026_05_13_chore_starlette_422_deprecation/)).
- **Manual operator handoffs (per `infra_foundation` §7.5):** `.env` is
  not auto-created (operator opts in via `cp .env.example .env`); OpenAI
  key file is empty by default; GitHub branch protection requires repo-admin
  action after the CI workflow lands.
- **No DB revision guard at API startup** in MVP1 (would crash the dev
  stack on first boot before `make migrate` runs). Activates at MVP2 when
  the API can assume the operator has run migrations once.
- **No remote staging** in MVP1 — every contributor runs the stack locally.
  Remote staging + production install land at MVP3.

## Quick-reference commands

```bash
# Stack lifecycle
make up            # generate secrets if missing, then docker compose up -d
make down          # stop containers (preserve volumes)
make logs          # tail api + worker
make reset         # DESTRUCTIVE: drop volumes + ./data (FORCE=1 to skip prompt)

# Migrations
make migrate                        # alembic upgrade head + init optuna schema
make migrate-create name=<slug>     # new alembic revision

# Tests + quality gates
make test-unit
make test-integration
make test-contract
make lint && make typecheck
make pre-commit                     # run all pre-commit hooks against the repo
```

## Where to look next

- [`architecture.md`](architecture.md) — high-level design + topical doc pointers
- [`CLAUDE.md`](CLAUDE.md) — codebase conventions, absolute rules, MVP1 status
- [`docs/03_runbooks/local-dev.md`](docs/03_runbooks/local-dev.md) — boot, debug, reset
- [`docs/05_quality/testing.md`](docs/05_quality/testing.md) — test layers + coverage gate
