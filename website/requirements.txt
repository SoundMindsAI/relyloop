# SPDX-FileCopyrightText: 2026 soundminds.ai
#
# SPDX-License-Identifier: Apache-2.0

# Build dependencies for the RelyLoop public site (https://relyloop.com).
# Pinned to exact versions for reproducible CI builds (see
# .github/workflows/deploy-docs.yml). Bump deliberately, not via `>=`.
#
# This is the CURATED top-level source. CI does NOT install from it directly —
# it installs the hash-pinned `website/requirements.lock` with
# `pip install --require-hashes` (OSSF Scorecard Pinned-Dependencies). After
# editing any pin below, regenerate the lock:
#
#     uv pip compile website/requirements.txt --generate-hashes -o website/requirements.lock
#
# The [imaging] extra (Pillow + CairoSVG) is included so social cards are a
# one-line opt-in, but the `social` plugin is intentionally NOT enabled in
# mkdocs.yml — it needs system Cairo libraries that the CI runner lacks.
# The `blog` and `tags` plugins ship inside mkdocs-material; no extra package.

mkdocs-material[imaging]==9.7.6
pymdown-extensions==10.21.3
# Pinch/click-zoom lightbox for the Guides screenshots
# (feat_website_walkthrough_guides). Honors the `glightbox-skip` class so
# index-card thumbnails navigate instead of opening an overlay.
mkdocs-glightbox==0.5.2
